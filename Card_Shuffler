#Shuffles the cards in a deck, then keeps drawing until either a Face Card or an Ace is found

from random import shuffle

cards = ['A',2,3,4,5,6,7,8,9,'K','Q','J']
shuffle(cards)
print(cards)

for c in cards:
  print(c)
  if c in ('K','Q','J','A'):
    break
